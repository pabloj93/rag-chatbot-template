fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-dotenv==1.0.1

# LangChain
langchain==0.3.7
langchain-anthropic==0.2.4
langchain-community==0.3.5
langchain-pinecone==0.2.0
langchain-text-splitters==0.3.2

# Embeddings (local, free)
sentence-transformers==3.2.1

# Vector DB
pinecone-client==5.0.1

# Anthropic SDK
anthropic==0.39.0
# Pinned: anthropic 0.39 calls httpx with the legacy `proxies=` kwarg,
# which httpx 0.28+ rejected. Keeps us on the last compatible release.
httpx==0.27.2

# Observability
langfuse==2.53.0

# Scraping (for Anthropic docs ingestion)
beautifulsoup4==4.12.3
requests==2.32.3
lxml==5.3.0

# BM25 keyword search (used by EnsembleRetriever in vectorstore.py)
rank-bm25==0.2.2
